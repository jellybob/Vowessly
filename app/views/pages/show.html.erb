<div class="block">
  <div class="content">
    <%= controls do |c|
      c.item "Edit Page", edit_page_path(page), :icon => "edit_page"
    end %>
    <h2><%= page.name %></h2>
    <div class="inner">
      <h3><%= page.content_type %></h3>
      <%= format_text page.body if page.body %>
    </div>
  </div>
</div>
